<% layout('layout') %>
<% stylesheet('/stylesheets/css/message.css')%>


<div class="backgroundUrl">
  <!-- nav -->
  <% include template/nav %>

  <div class="container-fluid">

    <div class="container-fluid">
      <div class="row">
        <h1 class="message_word m-3 col-lg-3 col-md-12 text-white"><%= title %></h1>


        <div class="col">
          <!-- 輸入 -->
          <% if (auth) { %>
            <form action="#" class="m-3">
              <div class=" form-group form-inline">
                <textarea class="message form-control col-md-7 col-sm-9 mr-1 col-xs-12 my-3" name="message" placeholder="安安你好"></textarea>
                <input type="submit" class=" form-control col-md-1 col-sm-2 col-xs-3 mb-3 align-self-end send" id="send" value="送出">
              </div>
            </form>
            <% } %>

            <!-- 已記錄的message -->
            <div class="row mt-5">
              <div class=" list col-lg-8 col-md-11 ml-2 mb-5">

              <% for (item in message) {%>
                <div data-id = <%= message[item].msg._id%> class=" bg-white p-3 border-bottom eachMessage m-1">
                  <div class="textBox">
                    <!-- 每一個twitter -->
                    <div class="messaged">
                      <%= message[item].nickname %> 說：
                      <%= message[item].msg.text %>
                    </div>
                  </div>

                  <!-- <div class="row"> -->
                    <!-- delete icon -->
    <!--                 <div class="update ml-auto edit mr-3">
                      <img src="/images/rubbish-bin.svg" alt="delete" class="text-secondary">
                    </div> -->
                  <!-- </div> -->

                </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<div>

<% script('../javascripts/newMsg.js') %>
